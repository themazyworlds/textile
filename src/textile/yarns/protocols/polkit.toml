[yarn]
name = "polkit"
publisher = "textile"
version = "1.0.0"
manifest_version = 1
layer = 50
description = "PolicyKit authority inspection and pkexec authorization rules"

[dependencies]
python = []
system = ["pkexec"]
