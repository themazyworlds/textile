[yarn]
name = "filesystem"
publisher = "textile"
version = "1.0.0"
manifest_version = 1
layer = 10
description = "Inotify file watching, disk usage, and storage mounts"

[dependencies]
python = []
system = []
